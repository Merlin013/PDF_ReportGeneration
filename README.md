This code is developed for extracting data(specific words and tables) from a pdf file and exporting the selected data to an excel file.

Further more the excel file will be used to generate pivot tables.